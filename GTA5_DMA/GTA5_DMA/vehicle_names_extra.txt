# 补充载具名表（免重编译）
# ---------------------------------------------------------------------------
# 内置表（Features/VehicleNames.h）来自公开数据 dump，共 921 辆，
# 比最新 DLC 旧 —— 新 DLC 车会显示哈希。想给它起名，在这里写一行即可：
#
#     模型名 = 显示名
#     模型名 = 显示名（分类）
#
# 模型名会按 joaat 算出哈希（与游戏内一致），不用填哈希。
# 井号开头为注释；空行忽略。
# ---------------------------------------------------------------------------

# —— 科茨中心抢劫 DLC（内置表未收录）——
velenogt = 维利诺 GT（超级跑车）
